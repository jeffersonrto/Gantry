# assets

`screenshot.png` — preview shown at the top of the project README.

**Pendente:** o screenshot atual ainda é o do plugin original (`DmsDockerManager`),
de antes do rebrand e da UI multi-runtime. Precisa ser substituído por um do
Gantry mostrando Docker e Podman lado a lado, com os badges de runtime visíveis.

Substituir este arquivo mantendo o nome `screenshot.png`; o README já aponta para ele.
